/*
condition =>
          -terneray condition is usefull
          -declare in a variable >then re-use it
          -directly use where need to 
          -avoid creating empty tags.
=> don't use condition inside a tag (when the tag can return emty value).

*/
