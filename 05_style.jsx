/* 
style  =>
        -css file import
        -js variable object(object key=style property,object value=style value)
        -using props

-using props =>
        we can use props in the js variable (as key) and add(the variable) by style in HTML opennig tag ,then pass the value in the component as usual

*/
